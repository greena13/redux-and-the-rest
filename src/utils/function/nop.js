/**
 * A empty function singleton that can be used whenever a function is required or expected, but no operation is
 * desired.
 */
// eslint-disable-next-line no-empty-function
const nop = () => {};

export default nop;
